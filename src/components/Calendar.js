/*
    logic breakdown
    mutate the GET url - specifically the date part of the url
    create 3 buttons
        - one to go back a previous day
        - on to go forward to the next day
        - reset 
    use if statement to restrict the number of days between months
*/
import React from "react";

const Calendar = () => {
    return (
        <div>
        
        </div>
    );
}

export default Calendar;